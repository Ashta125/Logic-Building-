# Logic-Building-
The User Login Check program is a basic Python authentication system that verifies whether the entered username and password match the stored credentials. If both are correct, it displays "Login Successful"; otherwise, it shows "Invalid Credentials". It demonstrates simple conditional logic.
